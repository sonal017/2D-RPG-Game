{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && python -m pip install -r requirements.txt",
  "buildCommand": ". .venv/bin/activate && python build_web.py",
  "outputDirectory": "build/web"
}
